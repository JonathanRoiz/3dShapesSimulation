A 3D shape renderer built from scratch in JavaScript — no libraries — that generates surface geometry, applies linear transformations for real-time rotation, and uses orthographic projection for rendering. Built with an object-oriented architecture where multiple shapes inherit from a shared base class using polymorphism.

[Live Demo](https://jonathanroiz.github.io/3dShapesSimulation/)
